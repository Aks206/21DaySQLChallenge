--Count the number of patients by each service.
SELECT 
  COUNT(*) AS number_of_patients,
  service
FROM patients
GROUP BY service
ORDER BY  COUNT(*) ASC

--Calculate the average age of patients grouped by service.

SELECT 
     ROUND(AVG(age),0) AS avg_age,
     service
FROM patients
GROUP BY service
ORDER BY AVG(age)


Find the total number of staff members per role.
SELECT 
     COUNT(*) AS total_number_of_staff, role
FROM staff
GROUP BY role

Question: For each hospital service, calculate the total number of patients admitted, total patients refused, 
and the admission rate (percentage of requests that were admitted). Order by admission rate descending.
SELECT 
     SUM(patients_admitted) AS total_patients_admitted,
     SUM(patients_refused) AS total_patients_refused,
	 ROUND(SUM(patients_admitted)*100/SUM(patients_request),0) AS percentage,
     service
FROM services_weekly
GROUP BY service
ORDER BY ROUND(SUM(patients_admitted)*100/SUM(patients_request),0) DESC
